"""Packet-local presentation assets for the Step 10 study report.

These are copied into every report packet rather than linked, because the
report is read from a durable work directory that may have no network. The
stylesheet is the ``html-doc-polish`` kit with its webfont import removed
for the same reason; the deviation is recorded at the top of the file.
"""
